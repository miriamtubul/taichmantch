# Taichmantch Project

אתר הזמנות לסלטי דגים ביתיים של גיל טייכמן (מושב תקומה).

## כלל ברזל

**כל שינוי חייב להיות תואם מובייל.** לפני כל שינוי ב-HTML או CSS — לוודא שהוא עובד גם במסכים צרים (320px+). לבדוק את ה-media query ב-`style.css` (max-width: 767px) ולעדכן אותו אם צריך.

## קבצים

- `index.html` — הדף הראשי (HTML + CSS + JS הכל בקובץ אחד)
- `style.css` — סגנונות נוספים
- `google-apps-script.js` — קוד להדביק ב-Google Apps Script

## Google Apps Script

- URL נוכחי (בשימוש ב-`index.html` שורה ~448):
  `https://script.google.com/macros/s/AKfycbxJaA2BY1ykFMf8JftMZZfu3toSS6kPOGhudaVyEuGQGpPaBqeXqHkz-nBqE5likoqC/exec`
- `doPost` — מקבל הזמנות ומוסיף שורה לגיליון הראשי
- `doGet` — מחזיר config (סלטים + נקודות איסוף + מחירים) מגיליון "הגדרות"
- תמיכה ב-JSONP לעקיפת CORS (האתר משתמש ב-fetch)

## גיליון "הגדרות" בשיטס

| עמודה | תוכן |
|-------|------|
| A | נקודות איסוף (שורות 2+) |
| B | סלטים (שורות 2+) |
| C2 | מחיר יחיד (ברירת מחדל: 35) |
| D2 | מחיר רביעייה (ברירת מחדל: 120) |
| E2 | מחיר שישייה (ברירת מחדל: 180) |

נוצר אוטומטית בפעם הראשונה שהדף נטען אחרי deploy.

## גיליון ראשי — עמודות

A=חותמת זמן, B=שם, C=טלפון, D=מייל, E=כתובת, F=נקודת איסוף,
G–L=סלטים בודדים (דינמי לפי מספר הסלטים),
אחרי הסלטים: רביעייה, שישייה, סה"כ פריטים, סה"כ ₪, הערות,
שולם, נמסר, נשלח לנקודת איסוף, תאריך איסוף, הערות גיל

**סלטים נוכחיים (6):** להבת הסלמון, מטיאס ים תיכוני, סלמון סקין, מטיאס חרדלי, סלמון קטיפתי, טונה ישראלית

**הוספת סלט חדש — חובה לעשות את שני הצעדים:**
1. הוסיפי את שם הסלט בגיליון "הגדרות" (עמודה B)
2. הוסיפי עמודה חדשה בגיליון "הזמנות" — **לפני** עמודת "רביעייה", ושמי בכותרת את שם הסלט. כל העמודות שאחריה יזוזו ימינה אוטומטית

## מחירים ולוגיקת חישוב

- סלט יחיד: 35 ₪
- רביעייה: 120 ₪ (4 סלטים בודדים = רביעייה אוטומטית)
- שישייה: 180 ₪ (6 סלטים בודדים = שישייה אוטומטית)
- לוגיקה: קודם שישיות מ-singles, אח"כ רביעיות מהשאריות, אח"כ בודדים
- משלוח עד הבית: +30 ₪
- ספירת פריטים: בודד = 1, רביעייה (q4) = 4, שישייה (q6) = 6

## Cache-busting

- `?v=X` מוצמד ישירות ב-HTML על CSS ותמונות (לא דרך JS!)
- **אסור** לשנות src/href של תמונות או CSS דרך JavaScript — זה גורם לטעינה כפולה והבזק מכוער
- כשמעדכנים CSS או תמונות: לשנות את `?v=2` ל-`?v=3` וכו׳ ישירות ב-HTML
- המקומות לעדכן: תג `<link>` של style.css + כל תגי `<img>` ו-`<image>` עם `pictures/`

## טעינה דינמית

- `loadConfig()` נקראת ב-DOMContentLoaded — טוענת config מ-Sheets ב-fetch
- fallback: אם fetch נכשל, ערכים סטטיים מה-HTML נשארים
- `applyConfig(config)` — מעדכנת סלטים, נקודות איסוף, מחירים
- **חשוב:** `applyConfig` בונה מחדש את כל ה-container כולל שורות רביעייה ושישייה
- `buildSaladRow(id, name, price)` — בונה שורת סלט דינמית

## עיצוב

- RTL עברית לאורך כל האתר
- צבעי סלטים: `SALAD_COLORS` — מערך של 8 צבעים מתחלפים
- צבעי סלטים נוכחיים: `#D96B5E`, `#4A7FA5`, `#5A9E72`, `#C4873A`, `#B07AA1`, `#C25B56`
- רביעייה: `#C49A2A` (זהב חם), שישייה: `#3A8B80` (טורקיז כהה)
- שורות רביעייה ושישייה עם class `qty-row--special` — צבע דרך CSS variable `--c`
- סלמון קטיפתי וטונה ישראלית: placeholder עיצובי (gradient + SVG) עד שיהיו תמונות
- CSS `color-mix` עם fallback לדפדפנים ישנים
