# Taichmantch Project

אתר הזמנות לסלטי דגים ביתיים של גיל טייכמן (מושב תקומה).

## קבצים

- `index.html` — הדף הראשי (HTML + CSS + JS הכל בקובץ אחד)
- `style.css` — סגנונות נוספים
- `google-apps-script.js` — קוד להדביק ב-Google Apps Script

## Google Apps Script

- URL נוכחי (בשימוש ב-`index.html` שורה ~373):
  `https://script.google.com/macros/s/AKfycbwV9H9vfacvye1yTqu25ZJ2RLF87Yq1DvOf7yJE9JuzKDA9IKoIT9g7w9ucPxt_xCqb/exec`
- `doPost` — מקבל הזמנות ומוסיף שורה לגיליון הראשי
- `doGet` — מחזיר config (סלטים + נקודות איסוף + מחירים) מגיליון "הגדרות"
- תמיכה ב-JSONP לעקיפת CORS

## גיליון "הגדרות" בשיטס

| עמודה | תוכן |
|-------|------|
| A | נקודות איסוף (שורות 2+) |
| B | סלטים (שורות 2+) |
| C2 | מחיר יחיד (ברירת מחדל: 35) |
| D2 | מחיר רביעייה (ברירת מחדל: 120) |

נוצר אוטומטית בפעם הראשונה שהדף נטען אחרי deploy.

## גיליון ראשי — עמודות A–S

A=חותמת זמן, B=שם, C=טלפון, D=מייל, E=כתובת, F=נקודת איסוף,
G=להבת הסלמון, H=מטיאס ים תיכוני, I=סלמון סקין, J=מטיאס חרדלי, K=רביעייה,
L=סה"כ פריטים, M=סה"כ ₪, N=הערות,
O=שולם, P=נמסר, Q=נשלח לנקודת איסוף, R=תאריך איסוף, S=הערות גיל

## מחירים ולוגיקת חישוב

- סלט יחיד: 35 ₪
- רביעייה: 120 ₪
- 4 סלטים בודדים אוטומטית = רביעייה (120 במקום 140)
- משלוח עד הבית: +30 ₪
- ספירת פריטים: כל סלט בודד = 1, כל רביעייה (q4) = 4

## טעינה דינמית

- `loadConfig()` נקראת ב-DOMContentLoaded — טוענת config מ-Sheets ב-JSONP
- timeout של 5 שניות — אם לא מגיע, ממשיך עם ה-HTML הסטטי כ-fallback
- `applyConfig(config)` — מעדכנת סלטים, נקודות איסוף, מחירים
- `buildSaladRow(id, name, price)` — בונה שורת סלט דינמית

## עיצוב

- RTL עברית לאורך כל האתר
- צבעי סלטים: `SALAD_COLORS` — מערך של 8 צבעים מתחלפים
- שורת הרביעייה תמיד אחרונה עם class `qty-row--special`
